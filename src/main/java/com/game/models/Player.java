package com.game.models;

public abstract class Player {
    //class fields/properties should include name, score, and gamesWon

    //All Arguments Constructor

    //Accessors and Mutators (getters and setters)

    // Optional: override the toString() method to print out a human-readable string of all the property values
}
