package com.game.models;

public class ComputerPlayer extends Player {
    // Class field called difficulty of type Difficulty (enum)

    // All arguments constructor that calls super()

    // Accessor and Mutator for difficulty
}
